export const WEB_URL = "http://localhost:5173";

export enum PagesNames {
    Admin = "/admin",
    Auth = "/autenticar",
    Profile = "/perfil",
    Teams = "/equipos",
    Error = "/error",
    Tournments = "/torneos"
}